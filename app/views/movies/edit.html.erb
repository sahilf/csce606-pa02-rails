<% content_for :title, "Editing movie" %>

<h1 class="page-title">Editing Movie</h1>

<div class="form-container">
  <%= render "form", movie: @movie %>
</div>

<div class="action-links">
  <%= link_to "Show this movie", @movie, class: "btn btn-back" %> |
  <%= link_to "Back to movies", movies_path(sort: session[:sort], direction: session[:direction]), class: "btn btn-back" %>
</div>
